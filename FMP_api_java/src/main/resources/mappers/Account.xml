<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.madhouse.fmp.domain.Account">

	<resultMap type="Account" autoMapping="true" id="AccountResult">
	</resultMap>
	
	<insert id="save" parameterType="Account">
		<selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id">
			SELECT LAST_INSERT_ID() AS id
		</selectKey>
		INSERT INTO account (username, password, firstName, lastName)
		VALUES(#{username}, #{password}, #{firstName}, #{lastName})
	</insert>
	
	<select id="getAccountByUserName" parameterType="java.lang.String" resultMap="AccountResult">
		select * from account where username = #{username}
	</select>

</mapper>


