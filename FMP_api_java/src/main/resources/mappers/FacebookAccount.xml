<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.madhouse.fmp.domain.FacebookAccount">

	<resultMap type="FacebookAccount" autoMapping="true" id="FacebookAccountResult">
	</resultMap>
	
	<insert id="save" parameterType="FacebookAccount">
		<selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id">
			SELECT LAST_INSERT_ID() AS id
		</selectKey>
		INSERT INTO facebook_account (ad_account_name, ad_account_id, access_token, ad_account_detail, want_sync, update_time)
		VALUES(#{adAccountName},#{adAccountId},#{accessToken},#{adAccountDetail},#{wantSync},#{updateTime})
	</insert>

</mapper>